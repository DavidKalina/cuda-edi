.DS_Store
.env
.env.*
!.env.example
node_modules/
dist/
*.log
.claude/

# The oately sandbox's own scratch at the checkout root: warm dependency cache,
# run logs, and a .env of credentials. Never ours to commit.
.sandcastle/
