{
  "name": "@cuda-edi/outbound-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Outbound FIFO worker (durable Lambda). Stub until #4; real OUTBOUND_214 in #5.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "node build.js",
    "build:tsc": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cuda-edi/edi-core": "0.1.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.7.9",
    "esbuild": "^0.25.0",
    "typescript": "~5.6.3"
  }
}
