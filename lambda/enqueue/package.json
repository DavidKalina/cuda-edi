{
  "name": "@cuda-edi/enqueue-lambda",
  "version": "0.1.0",
  "private": true,
  "description": "Enqueue Lambda: idempotent EDI Job persist + SQS FIFO handoff (implemented in #3).",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "node build.js",
    "build:tsc": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.758.0",
    "@cuda-edi/edi-core": "0.1.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.7.9",
    "esbuild": "^0.25.0",
    "typescript": "~5.6.3"
  }
}
